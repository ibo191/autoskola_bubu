# Local stage A only. No production credentials belong here.
APP_ENV=local
APP_ORIGIN=http://127.0.0.1:4321
# Set after starting the local Supabase stack. Never use a cloud URL.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
RECAPTCHA_ADAPTER=local
EMAIL_ADAPTER=local
ANALYTICS_ADAPTER=noop
# Unapproved legal texts keep order submission disabled.
LEGAL_APPROVED=false
